# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is an R/Quarto project for IPBES (Intergovernmental Science-Policy Platform on Biodiversity and Ecosystem Services) knowledge discovery. It uses the IPBES Linked Open Data (LOD) to extract the reference hierarchy from assessments, and snowball searching via the OpenAlex API to discover literature published after GA1 (2018).

## Build System

### targets Pipeline (primary)

```r
targets::tar_make()          # run all outdated targets
targets::tar_visnetwork()    # visualise dependency graph
targets::tar_outdated()      # list what needs re-running
```

**System dependency:** `fuseki-server` must be on `PATH` when `sparql_url: fuseki` (default). Install with `brew install fuseki`.

**Credentials:** `_targets.R` reads `API_openalex` and `API_openrouter` from the macOS keyring at startup via `keyring::key_get()`. Set them before running `tar_make()`:

```r
keyring::key_set("API_openalex")
keyring::key_set("API_openrouter")
```

**Running long `tar_make()` calls (e.g. `llm_verification_parquet`, `llm_verification_keypaper_parquet`) under tmux/SSH:** the `keyring::key_get()` calls above need macOS Keychain access every time `_targets.R` is sourced, including inside whatever process actually runs the pipeline. From a terminal session with no path to the GUI/WindowServer (SSH into the Mac — even to yourself — is enough to lose this, tmux inherits whatever its parent session has), a Keychain access prompt can't be shown and `tar_make()` fails outright with `keyring error (macOS Keychain), cannot get password: User interaction is not allowed`. This is a session/display-access issue, not a `callr`-subprocess one — passing `callr_function = NULL` does not fix it. Before starting a long background run this way:
1. `security unlock-keychain ~/Library/Keychains/login.keychain-db` in that same terminal (prompts for your login password there, no GUI needed).
2. If it still fails, the per-app access-confirmation dialog itself needs approving from an actual GUI session (physically at the machine, or Screen Sharing) — open Keychain Access.app, find `API_openalex`/`API_openrouter`, *Get Info* → *Access Control* → **Allow all applications** (or add the R binary and set **Always Allow**). Once granted, it persists across future headless/tmux runs.
3. As a last resort for one run, bypass keyring entirely: `export API_openrouter=<key>` in the shell *before* launching R, so `_targets.R`'s own `Sys.setenv(... = keyring::key_get(...))` line still runs but you've confirmed Keychain access via step 1/2 first — or comment that line out temporarily and rely on the exported env var alone.

**Not wired into `_targets.R` (source files exist but no active target):** `R/build_fulltext.R`, `R/resolve_citations.R`. Their corresponding outputs (`output/fulltext/`, `output/resolved_sections/`) are no longer produced by the live pipeline.

### Quarto Report

The report (`IPBES_Fact_Checker.qmd`) IS wired into the `targets` pipeline — `tar_make(report_fact_checker)` (or a plain `tar_make()`) builds every upstream target it needs and then renders it:

```r
targets::tar_make(names = "report_fact_checker")
```

`qmd_fact_checker` (file-hash tracked on `IPBES_Fact_Checker.qmd` itself) and `nli_bm_explorer_html`/`td_doc_html` (the interactive widgets and rendered design docs it links to) are all dependencies of `report_fact_checker`, so editing the qmd's prose, code, or YAML header, or any upstream data these embeds depend on, correctly invalidates the render. The report copies every heavy standalone HTML artifact it references (BM explorer widgets, overlap tables, rendered `TD_*` docs) into its own `IPBES_Fact_Checker_files/` sidecar directory at render time (via a `copy_into_report_files()` helper defined in the qmd's `setup` chunk) — so distributing the report only requires `IPBES_Fact_Checker.html` + `IPBES_Fact_Checker_files/`, not those source directories/files alongside it.

**Technical Design (`TD_<name>.qmd`) documents** are rendered the same way: each is a single self-contained file (the same `format: html` block as the main report, followed directly by the design doc's prose in the same file) so they get consistent styling and become standalone linkable HTML pages. `td_doc_names` (a plain character vector target) branches into `td_doc_qmd` (file-hash tracked) → `td_doc_html` (`quarto::quarto_render()` per branch) — adding a new TD doc is a one-line addition to `td_doc_names` plus the new `TD_<name>.qmd` file itself. The report's Methods section reads `td_doc_html` via `tar_read()` and links to the copied-in versions. (Previously these were split into a thin `.qmd` wrapper plus a separate single-source-of-truth `.md` file via `{{< include >}}`, so the prose would render nicely on GitHub's own file viewer and editors never touched Quarto YAML; combined back into one file per doc to simplify the pipeline — one fewer file-hash-tracked target (`td_doc_md`) and no `{{< include >}}` indirection.)

**`IPBES_Label_Funnel_Report.qmd`** (the per-(assessment, label, granularity) — REFUTES/SUPPORTS x naive_bm/complete_bm/atomic_bm — funnel report, linked from the main report's final section) is this project's **first use of Quarto's parameterized rendering** (`params:` in its YAML header + `execute_params =` on `quarto::quarto_render()`) — one shared `.qmd`, parameterized by `assessment_id`, `label`, and `granularity`, rendered once per (assessment, label) combination for whichever granularity that funnel branch was built for via `output_file = "IPBES_<LABEL>_Report_<id>_<granularity>.html"` (every granularity explicit, including `naive_bm` — changed from an earlier unsuffixed-default convention, since regenerating these reports is cheap regardless of which granularity is active), rather than branching over distinct source files the way `td_doc_html` does. This has one gotcha worth knowing before copying its setup chunk: the main report's `report_files_dir <- paste0(tools::file_path_sans_ext(knitr::current_input()), "_files")` idiom uses `knitr::current_input()`, which always returns the **source** `.qmd`'s name, not the `output_file=` override — copied verbatim into a parameterized report, every render would compute the same `_files/` directory and collide. `IPBES_Label_Funnel_Report.qmd` instead builds it from both params explicitly (`paste0("IPBES_", params$label, "_Report_", params$assessment_id, "_files")`). Since the underlying data/figures/tables live under label-specific target names (`refutes_funnel_data` vs. `supports_funnel_data`, etc., not one generic target), the qmd resolves which to read via `targets::tar_read_raw(paste0(tolower(params$label), "_funnel_data"))` — the string-based variant of `tar_read()`, needed because the target name itself is only known at render time.

## Architecture

### targets Pipeline Data Flow

```
input/config.yaml
    → output/LoD/<id>.ttl                    (downloaded IPBES LOD Turtle files, gitignored)
    → output/refs/assessment=<id>/           (partitioned by assessment, gitignored)
    → output/sections/assessment=<id>/       (partitioned by assessment, gitignored)
    → output/key_messages/assessment=<id>/   (partitioned by assessment, gitignored)
    → output/zotero/assessment=<id>/         (partitioned by assessment/group id/page, gitignored)
    → output/works/assessment=<id>/km=<km>/bm=<bm>/  (partitioned by assessment/km/bm, gitignored)
    → output/snowball/nodes/assessment=<id>/km=<km>/bm=<bm>/     (partitioned by assessment/km/bm/relation, gitignored)
    → output/snowball/edges/assessment=<id>/km=<km>/bm=<bm>/     (partitioned by assessment/km/bm/edge_type, gitignored)
    → output/snowball/keypaper/assessment=<id>/km=<km>/bm=<bm>/  (partitioned by assessment/km/bm, gitignored)
    → output/works_citing/assessment=<id>/km=<km>/bm=<bm>/       (partitioned by assessment/km/bm, gitignored)
    → output/nli_ready/assessment=<id>/km=<km>/bm=<bm>/          (work × BM-sentence cross-join; premise + bm_sentence ready for NLI, gitignored)
    → output/nli_scores/assessment=<id>/km=<km>/bm=<bm>/claim_id=<claim_id>/   (NLI SUPPORTS/REFUTES/NEI scores per citing work, gitignored)
    → output/llm_candidate_scope/assessment=<id>/km=<km>/bm=<bm>/             (per-claim citing-work match set, feeds the direct_evidence_match tag below, gitignored)
    → output/llm_verification/scores/llm_config=<cfg>/assessment=<id>/nli_route=<route>/km=<km>/bm=<bm>/   (Phase 2 LLM review of REFUTES/uncertain NLI pairs, each pair tagged direct_evidence_match, gitignored)
    → output/tables/{refutes,supports}_funnel_{data,table_l3}_<id>.{rds,html}, output/figures/fig_{refutes,supports}_funnel_{overall,by_bm,by_bm_normalized}_<id>.png   (3-level label sieve per assessment, gitignored)

# Not currently produced (orphaned source files in R/, gitignored outputs may hold stale data):
#   output/fulltext/, output/resolved_sections/
```

**Active scoring approach — NLI** (`TD_BM_NLI_approach.qmd`): `nli_scores_by_claim_evidence`
classifies each citing work against its partition's Background Message as
SUPPORTS / REFUTES / NOT_ENOUGH_INFO using a zero-shot NLI model
(`MoritzLaurer/bge-m3-zeroshot-v2.0-c` — `deberta-v3-large-zeroshot-v2.0` was
the original choice, now retired; see the `nli:` comment in
`input/config.yaml` for the full switch rationale) served on a pool of RunPod
hosts (see [external/runpod/docker/nli-runpod/](external/runpod/docker/nli-runpod/), a git
submodule — see `runpod_migration_IPBES_BM_Fact_Checker/TODO_migration_runpod.md`
for the migration off this repo's own former copy). Premise = cleaned `title`+`abstract`;
hypothesis = `bm_description`. The full probability distribution is stored; all
thresholding is done downstream in `dplyr`.

**Active Phase 2 — LLM verification** (`TD_NLI_LLM_two_phase.qmd`):
`llm_verification_parquet` reviews only the NLI pairs flagged `REFUTES` or
`uncertain`, one `ellmer::chat_openrouter()` call per `(claim, work)` pair via
OpenRouter, with a resumable per-pair cache and a post-hoc check that demotes
any verdict whose cited quote isn't actually verbatim in the supplied text.
Every citing work under a claim's BM is reviewed (there is no
candidate-narrowing filter — an earlier `subset: "all"`/`"default"`
mechanism was retired once real GA1 atomic_bm data showed the narrowing
reduced routed pairs by only ~14%, not worth the lost review coverage).
Instead each reviewed `(claim, work)` pair is tagged `direct_evidence_match`:
TRUE if that citing work traces back, via `llm_candidate_scope_parquet`, to
a seed reference whose sub-chapter matches the claim's own evidence-reference
braces (supported for `naive_bm`/`atomic_bm`; always FALSE under
`complete_bm`, which has no per-sub-claim evidence tokens). Its
architecture is ported from
the sibling `Categorisation_Literature` project's LLM epistemology
classifier (`R/llm_epistemology.R` there). This replaced an earlier
truth/citing-document LLM design
(`prompts_truth_parquet` → `prompts_citing_parquet` → `alignement_scores_*`,
driven by an `analysis:` config block) that was removed outright rather than
parked — see [TD_LLM_approach.qmd](TD_LLM_approach.qmd) for that record.

The `sparql_url` key in `input/config.yaml` controls the SPARQL backend:
- `"fuseki"` — each parquet builder starts an in-memory Fuseki session on a deterministic port and POSTs the assessment's TTL into the named graph returned by `assessment_graph_iri(id)`
- Any URL — the parquet builders query that endpoint directly; no Fuseki lifecycle needed

Config is split into fine-grained targets (`sparql_url`, `assessments_list`, `nli_active`/`nli_config`, `llm_verification_active`/`llm_verification_config`) so that adding new config sections or changing one entry does not invalidate other targets. Each top-level config key maps to its own intermediate target; `llm_verification:` follows the exact same `active` + named `configs` structure as `nli:` (three configs — `openrouter_cheap`/`openrouter_midtier`/`openrouter_toptier_gpt5` — carried over from the sibling `Categorisation_Literature` project's proven `epistemology:` config, including its `max_active: 24`/`max_tokens: 8000` values; see `llm_candidate_scope_parquet` below for the `direct_evidence_match` tag every config's output now carries instead of a `subset` filter).

**Per-assessment named graphs:** All three SPARQL queries wrap their patterns in `GRAPH <%GRAPH_IRI%> { ... }`. The placeholder is substituted at query time by `read_sparql_query()` in [R/extract_lod.R](R/extract_lod.R) using `assessment_graph_iri()` from [R/branch_helpers.R](R/branch_helpers.R) (currently `http://ontology.ipbes.net/report/<id>`). The same code path works against local Fuseki (TTL loaded into that graph at startup) and a shared IPBES endpoint hosting all assessments under the same IRI convention — switching backends is just a `sparql_url:` change in [input/config.yaml](input/config.yaml). If IPBES adopts a different convention, edit `assessment_graph_iri()` in one place.

### Legacy QMD Data Flow

```
input/Query_Submessage_ref_GA.csv
    → output/key_works.rds          (OpenAlex metadata for GA1 reference list)
    → output/snowballs/<id>.rds     (per-paper snowball results, gitignored)
    → output/nodes/ (parquet)       (all papers found, partitioned by bm/sm, gitignored)
    → output/edges/ (parquet)       (citation edges, partitioned by bm/sm, gitignored)
    → output/overlap_*.rds/.html    (overlap analysis results)
```

### Key Design Patterns

- **targets caching**: each target is re-run only when its inputs change. Delete `_targets/` to force a full rebuild.
- **Fine-grained config**: `config` is split into `sparql_url`, `assessments_list`, `nli_active`/`nli_config`, and `llm_verification_active`/`llm_verification_config` targets so unrelated config sections don't cascade invalidation.
- **Fuseki lifecycle**: started and stopped inside the parquet builders; cleanup is idempotent and handled by `on.exit()`.
- **Assessment branching**: `assessment` is the branch key, so adding a new assessment only computes the new branch.
- **Separated materialization**: refs, sections, key_messages, Zotero, works, snowball, works_citing, NLI scores, and LLM verification scores are all built independently; there is no cached combined `lod_data` object.
- **Parquet databases**: partitioned by assessment only, queried lazily with `arrow::open_dataset()` + `dplyr` verbs, collected into memory only when needed.
- **SPARQL queries as files**: all three SPARQL queries live in `queries/*.sparql` and are tracked as `format = "file"` targets (`refs_sparql`, `sections_sparql`, `key_messages_sparql`) — editing a query file invalidates only its downstream parquet target.
- **Legacy QMD caching**: uses `file.exists(fn)` checks. Delete the relevant `.rds` or parquet directory to force recomputation.

### R Files (targets pipeline)

| File | Target(s) | Purpose |
|------|-----------|---------|
| `R/download_works.R` | `works_parquet` | Fetch OpenAlex works via `openalexPro::pro_fetch()`, join with refs on DOI to assign km/bm, write to `output/works/assessment=<id>/km=<km>/bm=<bm>/` |
| `R/build_snowball_parquet.R` | `snowball_parquet` | Snowball search via `openalexSnowball::pro_snowball()` per km/bm; writes nodes and edges to `output/snowball/` |
| `R/build_works_citing_parquet.R` | `works_citing_parquet` | Filter snowball nodes to `relation == "citing"`; write to `output/works_citing/` partitioned by assessment/km/bm |
| `R/download_zotero.R` | `zotero_parquet` | Download Zotero group items to `output/zotero/assessment=<id>/` using refs parquet |
| `R/download_ttls.R` | `ttl_path` | Download TTL files to `output/LoD/`; SHA-checked against GitHub to skip re-download |
| `R/manage_fuseki.R` | helpers | Start/stop Fuseki sessions and resolve endpoints |
| `R/branch_helpers.R` | helpers | Assessment IDs and branch output paths. Also `nli_config_for_granularity(nli_configs, granularities, fallback)`: resolves, per granularity, the `nli.configs.<name>` entry that actually *produced* that granularity's scores (whichever config has a matching `granularity:` field), rather than assuming it's whichever config is currently `nli.active`. Needed because `nli.active` is a single global choice but `naive_bm`/`complete_bm`/`atomic_bm` are each normally scored under their own dedicated config — without this, switching `active` made `nli_overview_data`/`refutes_funnel_data`/`supports_funnel_data` (below) look under the wrong `nli_config=` subdirectory for every non-active granularity and silently report already-scored data as unscored. Falls back to `fallback` (`nli_active`) for any granularity with no config declaring it. |
| `R/render_diagrams.R` | `mmd_workflow_nli`, `diagram_workflow_nli`, `pipeline_mmd`, `diagram_pipeline_nli` | Render Mermaid `.mmd` sources to SVG. `workflow_nli.mmd` is the active, hand-authored conceptual workflow; `build_pipeline_mmd()` auto-generates `pipeline_nli.mmd` from the live `tar_mermaid()` DAG. The `_lm` variants of both (frozen snapshots of the earlier single-phase LLM-comparison approach) were removed once that approach's source was deleted outright rather than kept parked — see [TD_LLM_approach.qmd](TD_LLM_approach.qmd) |
| `R/extract_lod.R` | `refs_parquet`, `sections_parquet`, `key_messages_parquet` | SPARQL extraction helpers; reads queries from `queries/*.sparql` |
| `R/write_refs_parquet.R` | `refs_parquet` | Build DB1 directly into `output/refs/assessment=<id>/` |
| `R/write_sections_parquet.R` | `sections_parquet` | Build DB2 directly into `output/sections/assessment=<id>/` |
| `R/write_key_messages_parquet.R` | `key_messages_parquet` | Build DB3 directly into `output/key_messages/assessment=<id>/` |
| `R/clean_text.R` | helpers for `nli_ready_parquet` | `clean_text()`/`clean_title()`/`clean_abstract()` — strip HTML/JATS + LaTeX, squish whitespace. Copied verbatim from the TCAC 2.0 project. Deps: `xml2`, `stringr` |
| `R/build_nli_ready_parquet.R` | `nli_ready_parquet` | **Segmentation approach 1 (per-sentence).** Split each BM's `bm_description` (and/or `bm_label`) into sentences (fragments < 20 chars dropped), cross-join with the cleaned `premise` (= title + abstract) of every citing work. One row per (work × BM sentence) with `sentence_number`, `sentence_source`, `claim`, `premise`, and token estimates (`abstract_tokens`, `sentence_tokens`, `approx_tokens`). Parallelised over km/bm partitions with `parallel::mclapply` (`workers` from config). Writes to `output/nli_ready/assessment=<id>/km=<km>/bm=<bm>/` |
| `R/build_nli_ready_evidence_parquet.R` | `nli_ready_evidence_parquet` | **Segmentation approach 2 (per-evidence-reference), plus a `granularity`-selected alternative.** Identical schema/output either way. Default (`granularity: "naive_bm"`, the active config's own setting) cuts BM text into claims at brace evidence-references (`{5.4.1, 5.4.2}`) that *end a sentence* rather than at every sentence: `segment_bm_by_evidence()` accumulates sentences until one ends with a `{...}`, keeps trailing brace-less text (and whole brace-less fields) as a claim, and strips the braces from the hypothesis text. `granularity: "complete_bm"` instead uses `segment_bm_whole()` — no splitting at all, one claim per field (`bm_description`/`bm_label`), or one total if the two fields are identical after `stringr::str_squish()` (checked before segmenting; `naive_bm` is unaffected by this check). See NEXT_STEPS.md and TD_BM_NLI_approach.qmd (measured reduction + a hypothesis-length/model-token-limit caveat for `complete_bm`). Writes to `output/nli_ready_evidence/granularity=<naive_bm\|complete_bm\|atomic_bm>/assessment=<id>/km=<km>/bm=<bm>/` — `naive_bm`'s path is unchanged from before this partition level was added (existing data was migrated under `granularity=naive_bm/` via a one-time `mv`, not recomputed, so as not to invalidate 3.5M already-scored rows downstream). The downstream `nli_claim_units_evidence` → `nli_claim_units_evidence_flat` → `nli_scores_by_claim_evidence` chain reuses `build_nli_claim_units()`/`score_one_claim()` unchanged (only the source path and the `output/nli_scores_evidence` root, both granularity-partitioned the same way, differ). **A third value, `granularity: "atomic_bm"`,** uses `segment_bm_atomic()` — splits at *every* evidence brace, not just sentence-terminal ones (real GA1/A2 data showed `naive_bm` bundling several independently-evidenced semicolon-clauses into one claim, the same compound-claim problem `complete_bm` has at a smaller scale), then LLM-completes the resulting elliptical fragments via `complete_bm_fragments()` (`R/build_claim_completion.R`) using a `claim_completion_model` fine-grained config target. Both `naive_bm` and `atomic_bm` (not `complete_bm`, which has real scored data to protect) also extract IPBES's 4 standard confidence qualifiers ("well established"/"established but incomplete"/"unresolved"/"inconclusive") out of the claim text into their own `confidence` column via `extract_bm_confidence()`/`strip_bm_confidence()`, added via `dplyr::any_of()` so `complete_bm`'s output schema is untouched — found necessary after a real smoke test showed the completion LLM silently dropping a qualifier while rewording a fragment. See TD_BM_NLI_approach.qmd's "A third granularity" section for the full validation story. |
| `R/build_claim_completion.R` | (no target of its own — called from `nli_ready_evidence_parquet` under `granularity: "atomic_bm"`) | `complete_bm_fragments()`: LLM-completes elliptical `atomic_bm` fragments into self-contained claims, one `ellmer::parallel_chat_structured()` batch call per BM field, resumable per-fragment JSON cache under `output/claim_completion/raw/model=<model>/prompt=<hash>/` (same hash-covers-prompts-and-schema discipline as `build_llm_verification_parquet.R`). Reuses `build_llm_verification_chat()`/`load_text_file()`/`render_template()` from that file rather than duplicating them. `claim_completion_is_faithful()` checks every substantive (≥4-letter) word in a completion against the BM field's own original fragments and falls back to the uncompleted fragment on failure — same spirit as `quote_is_verbatim()`, but only catches *fabricated* content, not *omitted* content (which is why confidence qualifiers are extracted out before completion rather than relied on to survive it). Returns a tibble (`original_fragment`, `needs_completion`, `completed_claim`, `faithfulness_flagged`, `confidence` passthrough, `explanation`), not a bare character vector, so the atomic_bm QA report can show every field. Prompts: `input/prompts/claim_completion_system.md`/`claim_completion_user.md`. |
| `R/build_bm_split_highlighted.R` | `bm_split_report_highlighted` | QA view (not a scoring result) for how `nli_ready_evidence_parquet` actually split each BM into claims under whichever granularity is currently active — per (km, bm, sentence_source) field, the full original `bm_description`/`bm_label` text with each claim's source span colour-highlighted inline, followed by an indented, colour-matched list of the resulting claims (with their `confidence` qualifier, if any, shown on its own line). Highlighting is based on each claim's raw, pre-strip segment (small `*_raw()` sibling functions — `segment_bm_by_evidence_raw()`/`segment_bm_whole_raw()`/`segment_bm_atomic_raw()` — deliberately duplicate, rather than call, the three real segmenters in `R/build_nli_ready_evidence_parquet.R`, same reasoning `build_llm_candidate_scope_parquet.R` already documents for its own duplication of `segment_bm_by_evidence()`: even a behavior-preserving edit to one of the three real segmenters would mark `nli_ready_evidence_parquet` outdated), located inside the original text via a whitespace-flexible regex search rather than an exact substring match (naive_bm's multi-sentence accumulation runs each sentence through `str_squish()` before rejoining, so the reconstructed span isn't always byte-exact) — never the persisted `claim` text itself, which for `atomic_bm` can contain LLM-added words that never appeared in the source. Falls back to plain, unhighlighted text (via `warning()`, not `stop()`) whenever a field's raw segment count doesn't match its real persisted claim count or a segment can't be located; the claim list itself always comes from the real persisted data regardless. Reads `nli_ready_evidence_parquet`'s real on-disk output directly (`distinct()`-ed back down from the per-citing-work cross-join to one row per claim) rather than recomputing segmentation separately for the claims themselves, so it can never drift from what was actually produced. Rendered via `QA_BM_Split_Report.qmd` (`bm_split_report_html` target) — a QA artifact, not a `TD_*` design doc; unlike the DT-table version it replaced, this is plain HTML embedded directly in the report (`results: asis` + `cat()`), no separate widget file/iframe/sidecar. |
| `R/nli_http_helpers.R` | helpers for `nli_pool_health`, `nli_scores_by_claim` | Low-level HTTP helpers shared by health-checking and scoring: `nli_cfg_get()`, `nli_hosts()` (normalizes `host:` scalar-or-list to a character vector), `nli_classify_url()`, `nli_health()`, `nli_classify_request()`. |
| `R/check_nli_pool_health.R` | `nli_pool_health` | Health-checks every host in the active nli config's pool once per pipeline build. Fails loudly listing every unreachable host (never silently drops one); stops (not warns) if hosts report different models, since that would silently corrupt result provenance. Returns the common model name for stamping onto scored rows. |
| `R/build_nli_claim_units.R` | `nli_claim_units`, `nli_claim_units_flat` | Enumerates one unit per `(km, bm, sentence_source, sentence_number)` claim remaining to score for an assessment, sorted by pair count descending. The `approx_tokens <= max_length` filter here only affects **which rows are counted** for the largest-first ordering — it does **not** limit what gets scored (a claim with any in-budget row is enumerated, and `score_one_claim()` then scores every work of that claim; in practice all 600 claims are enumerated). Carries only identifying keys + the assessment's `nli_ready_parquet` path — **not** premises, so cached branch values stay small regardless of claim size. `nli_claim_units_flat` flattens the per-assessment lists into one combined list so the next target can branch one-target-per-claim across all assessments. |
| `R/score_one_claim.R` | `nli_scores_by_claim` | **Active NLI scoring**, one `targets` dynamic branch per claim (not per host/BM). Dynamic host dispatch: tries every host's file lock (`output/nli_scores/.locks/host_NN.lock`, via `filelock`) in turn — whichever host is free next gets the claim, so assignment happens when a worker becomes free rather than from an upfront LPT-style estimate. Local concurrency comes from a `crew_controller_local` (workers sized to the pool's host count, set in `_targets.R`'s `tar_option_set()`). Resumable: skips a claim immediately if its `claim_id=` output directory already has parquet files *and* that cached output's own `claim` column still matches the current claim text — `claim_id` is a structural key (`sentence_source`-`sentence_number`), not a hash of the text, so the same `claim_id` can legitimately hold different text across runs (a re-segmented boundary, a re-completed `atomic_bm` fragment); comparing the already-stored `claim` column (score_one_claim() always wrote this, so no schema change was needed) catches that and forces a rescore instead of silently serving stale text. `error = "continue"` on the target means a failing claim is reported live and marked failed while every other claim proceeds independently — no more losing an entire host's remaining backlog to one bad request, and no more silence until the whole build finishes (the old `mclapply`-per-host design's two failure modes). **Every work of a scored claim is scored** — `score_one_claim()` does not re-apply any token filter, so pairs longer than `max_length` are TRUNCATED by the server (`truncation="longest_first"` trims the abstract tail, preserving the short hypothesis), not skipped. See NEXT_STEPS.md for the optional abstract-chunking enhancement if lossless long-abstract handling is ever wanted. Writes to `output/nli_scores/nli_config=<cfg>/assessment=<id>/km=<km>/bm=<bm>/claim_id=<claim_id>/` |
| `R/find_orphaned_nli_scores.R` | *(no target — run by hand)* | Reconciliation utility for Phase 1's scored output, deliberately not wired into `_targets.R` since it can delete data. Nothing else in the pipeline notices a scored `claim_id` that no longer corresponds to anything in the current `nli_ready_evidence_parquet` (e.g. left behind by a since-changed segmentation) — `score_one_claim()`'s own resumability check only ever looks at its own claim, never at what else exists on disk, so such a claim would otherwise sit there indefinitely and keep silently contributing to `nli_overview_data`/the funnel targets' aggregate stats (both just `open_dataset()` the whole assessment tree with no filter tying rows back to the current claim list). `find_orphaned_nli_scores(nli_ready_path, nli_scores_path, delete = FALSE)` diffs on-disk `(km, bm, claim_id)` triples (`claim_id` alone is reused across every BM, so the comparison is always on the full triple, matching how `score_one_claim()` itself partitions) against what's actually in `nli_ready_path`, and reports (never deletes unless `delete = TRUE` is passed explicitly) anything orphaned. `find_orphaned_nli_scores_all()` runs this across every `(assessment, granularity)` combination, resolving each one's `nli_config` via `nli_config_for_granularity()` (`R/branch_helpers.R`) rather than assuming `nli.active`. |
| `R/build_nli_overview_data.R` | `nli_overview_data` | Per-assessment label/confidence/alignment summary tables (overall, per-KM, per-BM) read once from the on-disk scored-output path and cached as a single rds (`output/tables/nli_overview_data_<id>.rds`), so downstream figure/report targets don't re-`collect()` the raw dataset. Also caches trimmed per-row `raw` (km, bm, work_id, doi, label, confidence, alignment) — the input to `build_nli_bm_explorer()`; `work_id`/`doi` feed its drill-down table. `doi` comes from a left-join against `works_citing_path` (matched on `work_id == id`, both `https://openalex.org/W...` strings) — collapsed to one row per `work_id` first (picking any non-NA doi) since a work can appear once per km/bm partition it's cited from, so the join can't fan out and duplicate scored rows. **Wired to the evidence-segmentation chain only** (`output/nli_scores_evidence`, depends on `nli_scores_by_claim_evidence`), *not* the original per-sentence one (`output/nli_scores` / `nli_scores_by_claim`) — both scoring targets share `score_one_claim()`, so editing that file marks both outdated together regardless of which approach is actually being run, and the per-sentence chain has historically lagged far behind (e.g. 14/725 claims scored while evidence scoring was live). Wiring the report to the per-sentence target let `tar_make(report_fact_checker)` transitively try to dispatch hundreds of unscored per-sentence claims through the same live host pool as an in-progress evidence run — keep this pointed at the evidence chain unless you deliberately want to switch it back (and are prepared for `nli_scores_by_claim` to get pulled in). Its `nli_active` argument (used both to build the read path and to label/suffix the cached output) is resolved per-granularity via `nli_config_for_granularity()`, not passed `nli.active` directly — see that helper's own entry (`R/branch_helpers.R`) for why. |
| `R/build_nli_ready_evidence_keypaper_parquet.R` | `nli_ready_evidence_keypaper_parquet`, `nli_claim_units_evidence_keypaper`(`_flat`), `nli_scores_keypaper_evidence` | **QA sanity check, not part of the main scoring corpus**: scores the actual seed/reference papers IPBES cites as evidence for a BM (`relation == "keypaper"` in the snowball nodes — the same set as `works_parquet`, one row per `(paper, km, bm)` it's evidence for) against their own BM's claim text. A key paper *is* the evidence a BM was written from, so it should overwhelmingly land in SUPPORTS when scored — if it doesn't, that's a signal worth investigating, surfaced via the ternary figure's overlay (`R/build_nli_scores_qa_figures.R`, below). `build_nli_ready_evidence_keypaper_parquet()` is a structural twin of `build_nli_ready_evidence_parquet()` — same claims-building logic, calling that file's `segment_bm_by_evidence()`/`segment_bm_whole()`/`segment_bm_atomic()`/`complete_bm_fragments()` directly (a normal function call, not a copy of their regex logic) — but reads from the keypaper snowball partition instead of `works_citing_parquet`, and writes to its own `output/nli_ready_evidence_keypaper/` root, so it can never collide with or invalidate the existing, already-scored citing-works chain. `nli_claim_units_evidence_keypaper`/`_flat`/`nli_scores_keypaper_evidence` reuse `build_nli_claim_units()`/`score_one_claim()` completely unchanged — same "only the source path and output root differ" pattern the citing-works evidence chain itself already documents — writing to `output/nli_scores_evidence_keypaper/granularity=<g>/nli_config=<cfg>/assessment=<id>/...`. Single-active-granularity (not `cross()`'d over `nli_granularities`), matching the citing-works chain exactly; `nli_scores_qa_data` resolves whichever granularities actually have keypaper data the same way it already does for citing-works scores. **Runs automatically** — `nli_scores_qa_data` (below) takes `nli_scores_keypaper_evidence` as a bare (non-pattern) argument purely to establish the DAG dependency (branch shapes are incompatible for a real `pattern=` dependency: this chain is one dynamic branch per claim, `nli_scores_qa_data` is one branch per assessment × granularity — same convention `build_llm_verification_parquet()` already uses for its own `nli_scores_by_claim_evidence` argument), so a plain `tar_make()`/`tar_make(names = "report_fact_checker")` now dispatches real key-paper NLI classification calls too, not just the main citing-works corpus — **not safe to render blind** for the same reason `llm_verification_parquet` isn't (see that row below); use `tar_make(names = ..., shortcut = TRUE)` to render against on-disk data without triggering a fresh run. Note this target is tied to whichever granularity is *currently* `nli.active` (same as the citing-works chain) — to score key papers against the `complete_bm` view specifically, `nli.active` must point at a `complete_bm` config when this runs. |
| `R/build_nli_scores_qa_data.R` | `nli_scores_qa_data` | **QA report for Phase 1 (scoring) itself** — sibling to `R/build_bm_split_highlighted.R` (which QAs the segmentation step); this QAs whether the zero-shot NLI classifier's scores look sane. Same `cross(assessment, nli_granularities)` scoping and `nli_config_for_granularity()` resolution as `nli_overview_data` just above (each granularity is normally scored under its own dedicated config, not whichever one `nli.active` currently points to). `nli_scores_evidence` never stores title/abstract/doi (only `work_id`), so `build_nli_scores_qa_data()` joins those back from `works_citing_parquet` — but only *after* capping (see below), so the join never touches the full ~1-2M row scored table, only whatever survives. Computes two things, both on the **full, uncapped** data except the second: (1) a REFUTES/SUPPORTS/NOT_ENOUGH_INFO x confidence-decile count matrix (nothing else in the repo crosses label against a confidence band); (2) a capped per-`(km, bm, claim_id)` table — top `per_claim_cap` (default 50) rows by confidence descending, with each claim's true row count carried as `n_total_claim` so truncation is never silent (same ethos as `build_nli_bm_explorer.R`'s `table_row_cap`/`download_row_cap`). The cap default was set by direct measurement against real GA1 complete_bm data: 200/claim rendered a 30.6MB self-contained HTML (comparable to, but not much smaller than, `nli_bm_explorer_html`'s own ~37MB); 50/claim renders ~8.5MB, a more comfortable size for what's meant to be a spot-check view, not a full corpus browser (`nli_bm_explorer_html` already is one). The DT widget itself (`nli_scores_qa_datatable()`, same file) is built and cached **inside this function**, not inside the qmd — `quarto::quarto_render()` runs `QA_NLI_Scores_Report.qmd` in a fresh session that never sources `R/*.R` (same reasoning `IPBES_Label_Funnel_Report.qmd`'s setup chunk gives for inlining `granularity_suffix()`/`nli_model_suffix()` rather than sourcing `R/branch_helpers.R` there), and a plain `DT::datatable()` object is a self-contained htmlwidget with no captured R closures, so caching the already-built widget and having the qmd just auto-print it avoids both the cross-session-sourcing problem and duplicating this function's logic a second time inline. Bypasses `IPBES.R::table_dt()` for the same reason `build_label_funnel_tables.R` does (`filter = "top"` is a top-level `datatable()` arg its wrapper can't forward). Rendered via `QA_NLI_Scores_Report.qmd` (`nli_scores_qa_report_html` target), filename `QA_NLI_Scores_Report_<id><_nli_config><_granularity>.html`. **Cold-build quirk** (reproduced directly, not hypothetical): if `nli_scores_qa_data` has never been built before, building it and `nli_scores_qa_report_html` in the *same* `tar_make()` call can race — a branch of the latter can start rendering (and its qmd's `tar_read_raw("nli_scores_qa_data")` call fail with "target ... not found") before `nli_scores_qa_data`'s dynamic-branch pattern is fully finalized in `_targets/meta/meta`, even though every individual branch already completed. Simply re-running `tar_make()` fixes it. The same risk likely applies to the pre-existing `refutes_funnel_data`/`report_refutes_funnel_html` pattern this mirrors — just never hit because those targets are rarely both cold in the same call in practice. Also caches `label_pct` (real % of rows won by each label, from the `label` column directly), `probs` (the full uncapped `p_supports`/`p_refutes`/`p_nei`, ~14MB for 594K rows), and `uncertain_threshold` (the resolved granularity's own config value, passed in by the caller the same way `nli_config_for_granularity()` itself is used — not `nli.active`'s) — all three feed the ternary figure below (the last one draws its certain/uncertain boundary lines), kept here rather than re-collected/re-resolved there so it's read once. Also takes `nli_scores_keypaper_evidence` as a bare (DAG-dependency-only, unused in the body) argument so the key-paper scoring chain runs automatically as part of a plain `tar_make()` rather than needing an explicit trigger — see that target's own row above. Reads `keypaper_scores_path` (present once that chain has actually built, absent on a cold first build before it's ever run) to build `keypaper_points` (each key paper's own probabilities, collapsed to one row per `(work_id, km, bm)` — mean of `p_supports`/`p_refutes`/`p_nei` across whatever claims exist under that BM, since a key paper is evidence for the BM as a whole, not one specific claim within it; no claim-level evidence-to-paper mapping exists yet, so under `complete_bm`'s 2 claims per BM the same `(paper, BM)` reference would otherwise plot as two near-duplicate overlapping points — verified directly: 10 synthetic rows, 5 papers × 2 claims, collapsed to exactly 5), `keypaper_summary` (overall % landing in SUPPORTS, for the report's validation callout), `keypaper_label_pct` (same shape as `label_pct` but for key papers — feeds the ternary figure's bracketed per-corner percentage), and `keypaper_label_pvalue` (one two-proportion `stats::prop.test()` p-value per label, key-paper share vs. full-corpus share — `label_n`/`keypaper_label_n`, the raw counts underlying `label_pct`/`keypaper_label_pct`, are reindexed to all 3 `nli_label_levels` first so a label with zero rows never silently drops out of the test) for the ternary figure's overlay; all four stay `NULL` — not an error — until that chain has actually been run. (An earlier version also classified each point as single-BM vs shared-across-multiple-BMs with two marker shapes; dropped for simplicity — see `R/build_nli_scores_qa_figures.R`'s row below.) |
| `R/build_nli_scores_qa_figures.R` | `nli_scores_qa_figures` | One static PNG from `nli_scores_qa_data`, via `nli_scores_qa_ternary_plot()`: a ternary (simplex) density plot of the full `(p_supports, p_refutes, p_nei)` distribution — since the three always sum to 1, every row lives on a 2D triangular surface, not a 3D volume, so this is the standard lossless 2D visualization for 3-part compositional data (a literal 3D scatter was considered and rejected: mostly shows the same flat triangle from an angle, with occlusion/no-interactivity problems on top, since these render as static PNGs). Density via `MASS::kde2d()`, `log1p`-transformed before contouring — a linear scale left almost the whole triangle (e.g. the entire REFUTES-heavy region) in one flat lowest bin, hiding real structure under a couple of much taller peaks elsewhere. The three decision-boundary lines (where the argmax label flips) run from the **centroid to each edge's midpoint only**, not from the vertices — verified geometrically (sampled points exactly along a vertex-to-centroid segment and confirmed the whole segment stays inside one already-winning region, i.e. it's not an actual boundary between two regions at all; drawing it anyway would be technically-valid-but-misleading). Each corner's label carries the real % of rows that label won, from `label_pct` — plus, when `keypaper_label_pct` is present, the key papers' own share for that label in square brackets right after it, followed by the `keypaper_label_pvalue` for that label (e.g. "REFUTES (12.3%) [8.0%, p<0.001]") when present — `fmt_pvalue()` formats it as `p<0.001` below that threshold, else `p=0.023` to 3 decimals, deliberately not collapsed to significance stars so the reader sees the actual number. When `keypaper_points` is present, overlays the actual key papers as small white-filled, black-bordered points (single shape — an earlier version distinguished single-BM vs shared-across-multiple-BMs papers with two marker shapes, dropped since it added more visual complexity than the distinction was worth); kept deliberately small (`size = 0.6`) — a larger version visibly obscured the density surface underneath at any real key-paper count. `uncertain_threshold` (default 0.60) draws the **certain/uncertain boundary** — three lines, each parallel to the edge opposite one vertex at that vertex's threshold value, using the same construction as the plain 20/40/60/80% gridlines (`gridline_df()`, called a second time for the single `uncertain_threshold` level) but styled black/dashed with its own `scale_linetype_manual()` legend entry (a separate ggplot aesthetic/scale from the coloured decision-boundary lines, so the two legends coexist without needing `ggnewscale`) rather than reading as just another generic gridline — the three small corners beyond it are `certain` (score_one_claim.R's own `confidence >= uncertain_threshold`), the hexagonal middle is `uncertain` regardless of which label technically won there. A sibling line chart (`p_supports`/`p_refutes`/`p_nei` binned by decile against mean `confidence`) previously lived here too; removed as not worth the space once the ternary plot covered the same probability/confidence relationship more directly, though `uncertain_threshold` itself was later restored to `build_nli_scores_qa_data()`'s output for this boundary-line use (`prob_conf_curve` stays gone). Separate target from `nli_scores_qa_data` itself — same split as `nli_overview_data` → `nli_overview_figures` — so replotting doesn't require recollecting the raw scored table. Embedded into `QA_NLI_Scores_Report.qmd` as a base64 `data:` URI via `xfun::base64_uri()`, not a plain markdown `![]()` path or a `copy_into_report_files()` sidecar — confirmed directly that Quarto/pandoc's own resource resolution mangles even an absolute path here (silently turned `/Users/.../x.png` into `./Users/.../x.png`, then warned the resource couldn't be fetched, rather than erroring loudly); encoding to a data URI sidesteps that path resolution entirely and needs no sidecar directory. |
| `R/build_llm_candidate_scope_parquet.R` | `llm_candidate_scope_parquet` | **Per-claim evidence match set** feeding Phase 2's `direct_evidence_match` tag (this used to power a `subset: "default"`/`"all"` candidate-narrowing filter — retired once real GA1 atomic_bm data showed the narrowing reduced routed pairs by only ~14%, not worth the lost review coverage; every citing work is now reviewed regardless, just tagged). Chains `refs_parquet`'s `sm` (sub-chapter id, exploded and normalised — lowercased, `box`/`table`/`spm table` prefixes and `(...)` annotations stripped, comma/semicolon-split) to seed `doi` to seed OpenAlex work id (`works_parquet`) to citing work (`output/snowball/edges`, filtered to `to %in%` the in-scope seed ids), matched against each claim's own brace tokens (`{5.4.1, 5.4.2}`) via `evidence_tokens_match()`'s prefix rule (`"5.4.2.1"` matches `"5.4.2.1.a"`; bare `"4"` matches any `"4.x.y"`). Two granularity-specific extractors supply those brace tokens: `extract_claim_evidence_tokens()` (for `naive_bm`) deliberately **duplicates** (does not call) `segment_bm_by_evidence()` from `build_nli_ready_evidence_parquet.R`, and `extract_claim_evidence_tokens_atomic()` (for `atomic_bm`) likewise duplicates `segment_bm_atomic()` — even a behavior-preserving edit to either real segmenter would mark `nli_ready_evidence_parquet`, and the NLI scoring chain downstream of it, outdated, since `targets` hashes function bodies as dependencies. Atomic_bm's real `claim_id`/`sentence_number` depends on which raw fragments *survive* `complete_bm_fragments()` (a fragment can be dropped, never reordered), so `extract_claim_evidence_tokens_atomic()` additionally re-runs completion and re-derives the surviving order via `bm_align_surviving_indices()` (the same alignment helper `build_bm_split_highlighted.R` uses for its own atomic_bm highlighting, called directly here since it isn't one of the three segmenters this file avoids calling) — relying on the resumable per-fragment cache (`output/claim_completion/raw/`) to make this a cache-hit-only call once a real atomic_bm `nli_ready_evidence_parquet` build has already populated it; needs `claim_completion_model`/`API_openrouter`, `stop()`s loudly if the key is missing. Cross-checks its own derived `claim_id`s against `nli_ready_evidence_parquet`'s real ones at build time and `warning()`s (not `stop()`s) on any mismatch, guarding against the segmentation implementations silently drifting apart later. A claim with no evidence braces at all is simply absent from the output — the consumer (`build_llm_verification_parquet.R`) tags every one of its citing works `direct_evidence_match = FALSE`, never excludes them. Takes the active config's `nli_granularity` and, when `"complete_bm"`, returns the same "no matches available" empty sentinel immediately — there's no `complete_bm` counterpart to either extractor, so per-sub-claim evidence tagging simply isn't supported for complete-BM's whole-field claims (documented limitation, not silently-wrong output; every row is tagged FALSE under `complete_bm`). Reads only already-existing, unmodified targets; under `naive_bm`/`complete_bm` makes no network/API calls, so adding it does not invalidate `download_works.R`/`build_snowball_parquet.R`/`build_works_citing_parquet.R`/`build_nli_ready_evidence_parquet.R`/the NLI scoring chain. Always computed regardless of which `llm_verification` config is active. Writes to `output/llm_candidate_scope/assessment=<id>/km=<km>/bm=<bm>/`. |
| `R/build_llm_verification_parquet.R` | `llm_verification_parquet` | **Phase 2 LLM verification**, one call per assessment (not per claim — no crew/lock dispatch needed since OpenRouter is a shared endpoint, unlike the fixed RunPod pool Phase 1 load-balances across). `select_llm_verification_candidates()` filters Phase 1's scored output per the active config's own `nli_labels` (`label %in% nli_labels`, `NULL`/absent = any) and `nli_certainty` (`"certain"`/`"uncertain"`/`NULL` = any) fields, combined with AND — every shipped config currently sets `nli_labels: [REFUTES, SUPPORTS]`, `nli_certainty: certain` (`REFUTES`+`certain` was the original first step, 26,861 pairs; `SUPPORTS`+`certain` was added once the label funnel reports below were generalized to cover both, 52,061 more pairs — 78,922 of 3,547,231 scored pairs combined) — then joins back `premise` text from `nli_ready_evidence_parquet` (Phase 1's own output never stores premise text). Every routed candidate is reviewed — `tag_direct_evidence_match()` tags each one against `llm_candidate_scope_parquet` (matched on the full `(km, bm, claim_id, work_id)` key, not `claim_id` alone — `claim_id` strings like `"bm_description-01"` are reused across 30-49 different BMs per assessment, so matching on `claim_id` alone let one BM's scope entries wrongly authorize another's candidates until this was caught and fixed) rather than filtering by it, since real GA1 atomic_bm data showed the old `subset: "default"` filter only narrowed routed pairs by ~14% — not worth the lost review coverage; a claim absent from the scope table is simply tagged `direct_evidence_match = FALSE` throughout. One `ellmer::chat_openrouter()` call per routed `(claim, work)` pair, architecture ported from the sibling `Categorisation_Literature` project's `R/llm_epistemology.R`: resumable JSON cache under `output/llm_verification/raw/model=<model>/prompt=<hash>/assessment=<id>/`, one file per `(km, bm, claim_id, work_id)` (hash covers system prompt + user template + output schema, so editing any of them starts a fresh cache namespace — deliberately **not** keyed by routing fields or the `direct_evidence_match` tag, since a cached answer for a given `(model, prompts, schema)` pair is valid regardless of which routed bucket brought it there or how it's tagged). The cache key is the full `(km, bm, claim_id, work_id)`, not `claim_id`+`work_id` alone — same `claim_id`-is-only-unique-within-one-BM bug class as `tag_direct_evidence_match()` below, caught before Phase 2 was ever run for real by measuring collisions directly against the real routed corpus (11-40% of rows depending on assessment/granularity under the old key) — plus its own `assessment=<id>/` path segment, since the cache directory isn't otherwise assessment-scoped. A retry pass re-checks only the still-failing subset each pass (not a full corpus rescan), and a fail-loud check fires if more than half the pairs return unparseable responses (a broken-plumbing signal, not honest abstention); a candidate that never produces any cache entry at all (as opposed to a recorded `failed = TRUE`) is now also surfaced via `warning()` rather than silently vanishing from output. `quote_is_verbatim()` then checks every LLM-cited quote actually occurs (normalised comparison) in the premise text shown to it, demoting `sufficient_evidence`/`llm_label` to `NOT_ENOUGH_INFO` if not — this is the piece most worth having ported rather than reinvented. Writes to `output/llm_verification/scores/llm_config=<config>/assessment=<id>/nli_route=<route>/km=<km>/bm=<bm>/`, where `nli_route` is `nli_route_label()`'s hive-safe encoding of **that row's own** NLI outcome (e.g. `REFUTES-certain`), computed per row from its actual label/uncertain values rather than once per config call — so a config listing several labels/certainties (e.g. `nli_labels: [REFUTES, SUPPORTS]`) still writes one `nli_route=` subdirectory per distinct outcome actually present (`nli_route=REFUTES-certain` and `nli_route=SUPPORTS-certain`, never a merged string), and switching `active`, widening routing fields, or comparing several configs side by side never overwrites another combination's scored rows — same reasoning as `nli_config` in Phase 1's own output path. Configured via `input/config.yaml`'s `llm_verification:` block (`active` + named `configs`, mirroring `nli:`). **No longer safe to render blind**: once the label funnel reports (`refutes_funnel_data`/`supports_funnel_data`, below) started reading this target's output and got folded into `report_fact_checker`'s own dependency list, `report_fact_checker` became transitively dependent on it — a plain `tar_make()`/`tar_make(report_fact_checker)` will rebuild it (spending real OpenRouter money) whenever `llm_verification_config` is outdated. Use `tar_make(names = ..., shortcut = TRUE)` to render against on-disk Phase 2 data without triggering a fresh run; see TD_NLI_LLM_two_phase.qmd's "Where this sits in the pipeline" section. Its output is not yet merged into `nli_overview_data` — see TD_NLI_LLM_two_phase.qmd's Recommended Workflow. |
| `R/build_llm_verification_keypaper_parquet.R` | `llm_verification_keypaper_parquet` | **Full-coverage LLM verification of key papers** — QA sanity check, NOT the main Phase 2 corpus. Reviews **every** key/seed paper's NLI-scored pair, irrespective of `nli_labels`/`nli_certainty` (unlike the main citing-works chain above, which routes by those fields purely for cost control — key papers are a small, bounded, high-importance set where every one is worth an independent check). Structural sibling of `build_llm_verification_parquet.R`, same "separate file, don't touch the delicate already-working builder" precedent `R/build_nli_ready_evidence_keypaper_parquet.R` itself documents for its own Phase-1 counterpart — doubly justified here since that function was only just stabilized against a `pair_id` collision bug. **Reuses directly** (calls, doesn't duplicate) the small generic helpers already in `build_llm_verification_parquet.R` — `build_llm_verification_chat()`, `llm_verification_output_type()`, `normalise_llm_verification()`, `quote_is_verbatim()`, `render_template()`, `load_text_file()`, `nli_route_label()`, and critically `select_llm_verification_candidates()` itself, which is already fully path-parameterized and already supports "no filter" via `nli_labels = NULL, nli_certainty = NULL`. **Does duplicate** the chunking/`run_pairs()`/retry/cache-write/output-assembly orchestration loop, adapted to the keypaper paths. No `direct_evidence_match` tagging — that concept (does a *citing* work trace back to a seed reference matching the claim's evidence braces) doesn't apply to a key paper, which *is* a seed reference itself; column omitted from this chain's output schema entirely. Reads premise from `nli_ready_evidence_keypaper_parquet` and NLI scores from `output/nli_scores_evidence_keypaper/granularity=<g>/nli_config=<cfg>/assessment=<id>/`. Writes to its own `output/llm_verification/scores_keypaper/` root (cache under `output/llm_verification/raw_keypaper/`) — never collides with or invalidates the main citing-works output tree. Takes `nli_scores_keypaper_evidence` as a bare (DAG-dependency-only) argument, same convention `llm_verification_parquet`/`nli_scores_qa_data` already use for their own upstream keypaper/Phase-1 dependencies — runs automatically as part of a bare `tar_make()`. **New paid API surface**: same "no longer safe to render blind" caution as `llm_verification_parquet`/`nli_scores_keypaper_evidence` — `tar_make(names = ..., shortcut = TRUE)` renders against on-disk data without triggering a fresh run. |
| `R/build_llm_verification_qa_data.R`, `R/build_llm_verification_qa_figures.R` | `llm_verification_qa_data`, `llm_verification_qa_figures`, `llm_verification_qa_report_html` | **QA report for Phase 2 (LLM verification)** — sibling to `R/build_nli_scores_qa_data.R`/`R/build_nli_scores_qa_figures.R` (which QA Phase 1). Single-active-granularity, **not** `cross()`'d over `nli_granularities` like `nli_scores_qa_data` is — `llm_verification_parquet` only ever reflects whichever granularity is currently active, so there's nothing to cross. `llm_verification_parquet`'s own output already carries everything needed per row (no need to re-join Phase 1's scored table); only `title`/`abstract`/`doi` get joined in, from `works_citing_parquet` for citing works and from `works_parquet` for key papers (key papers are SEED works, not in the citing-works dataset — a distinct lookup path from the citing-works one). Computes an `nli_label` × `llm_label` contingency (feeds both the confusion-matrix table and the alluvial figure — computed once as `label_flow`, reused both places) and a per-NLI-confidence-decile `llm_agrees` rate (feeds the decile figure), plus a capped-per-`(km, bm, claim_id)` DT table (`llm_verification_qa_datatable()`, same never-silent-truncation `n_total_claim` convention as `nli_scores_qa_datatable()`) and, when `llm_verification_keypaper_parquet` has data, the key-paper validation fields (`keypaper_n`, `keypaper_llm_supports_pct`, `keypaper_llm_agrees_pct`, an **uncapped** `keypaper_flagged_widget` DT table of every key paper the LLM did *not* confirm as SUPPORTS) plus the two figures' key-paper overlay inputs. **A literal ternary plot (Phase 1's own QA figure) was considered and rejected**: the LLM emits one categorical verdict, not a 3-part probability composition, so a simplex density would really just be NLI's own chart wearing a Phase-2 label. Two figures instead, both built from what Phase 2 actually produces, in a **new file** — deliberately does **not** modify `R/build_nli_scores_qa_figures.R`/`nli_scores_qa_ternary_plot()`, same "duplicate, don't call" discipline protecting Phase-1-adjacent (explicitly locked) code from unrelated invalidation (`nli_label_colors`/`nli_label_levels` from `R/branch_helpers.R` ARE reused directly — generic shared constants, not corpus-protecting): (1) `llm_verification_qa_decile_plot()` — X = NLI-confidence decile, Y = % of reviewed pairs where `llm_agrees`, one line + point-size-by-n, with a key-paper overlay using the **same** `llm_agrees` metric (individual jittered points at each key paper's own confidence, plus a dashed aggregate reference line) so the two series are directly comparable on one y-axis — the "% confirmed SUPPORTS" framing is deliberately kept out of this chart's metric and lives in the report's plain-text summary bullets instead; (2) `llm_verification_qa_alluvial_plot()` — an `nli_label` → `llm_label` flow diagram (`ggalluvial::geom_alluvium()`/`geom_stratum()`, a **new dependency**, already installed but not previously referenced in this project — no `renv.lock`/`DESCRIPTION` exists here to update) coloured by agreement, with key papers shown as a **side-by-side mini-alluvial** (`patchwork::wrap_plots()`, also new) rather than an overlaid second flow, since key-paper `n` is typically orders of magnitude smaller than the full reviewed corpus and would be visually invisible on the main flow's scale. Empty-state guard checks `dir.exists()` **and** that the path actually contains `.parquet` files (same pattern `tag_direct_evidence_match()` uses), not `dir.exists()` alone, since `llm_verification_parquet`/`llm_verification_keypaper_parquet` can return their `output_path` even when nothing was written. Rendered via `QA_LLM_Verification_Report.qmd`, filename `QA_LLM_Verification_Report_<assessment>_<llm_config>.html` (`llm_config` always suffixed — no unsuffixed-default exception, unlike `nli_model_suffix()`'s historical one for `"deberta_zeroshot"`). |
| `R/build_nli_overview_figures.R` | `nli_overview_figures` | Static ggplot PNGs per assessment: overall/per-KM/per-BM label split, confidence density, alignment density. Writes to `output/figures/`. |
| `R/build_label_funnel_data.R` | `refutes_funnel_data`, `supports_funnel_data` | `build_label_funnel_data(assessment, target_label, ..., granularity)` counts a 3-level sieve per (assessment, granularity) combination for one NLI label (`"REFUTES"` or `"SUPPORTS"`), each level a **subset** of the previous, scoped to distinct citing works per `(km, bm)` (a work matching two BMs is two findings, so per-BM counts sum exactly to the assessment-wide total): (1) every work in `works_citing_parquet` for that BM, (2) of those, `label == target_label & !uncertain` in `nli_scores_evidence`, (3) of those, `llm_agrees == TRUE` in `llm_verification/scores` (explicitly re-filtered to `nli_route == paste0(target_label, "-certain")`, defensively, in case a future config broadens routing to other outcomes sharing the same output tree). Called once per (label, granularity) from `_targets.R` (`refutes_funnel_data`/`supports_funnel_data`, `pattern = cross(map(assessment, ...), nli_granularities)` — one shared implementation rather than per-combination copies that would drift apart; `nli_granularities` is a fixed `c("naive_bm", "complete_bm", "atomic_bm")`, not read from config, so all three always render even though only one is ever "active" for Phase 1 scoring itself). The `nli_config` used to locate (and label) each granularity's data is resolved per-branch via `nli_config_for_granularity()` (`R/branch_helpers.R`), not `nli_active` directly — each granularity is normally scored under its own dedicated config, so substituting the single globally active config name for every branch would make an already-scored, non-active granularity look unscored the moment `nli.active` points elsewhere. Output filename gets a `_<granularity>` suffix for anything other than `naive_bm` (`granularity_suffix()`, `R/branch_helpers.R`) so existing `naive_bm` filenames/links are untouched. A 4th level, `sufficient_evidence == TRUE`, was considered for the original REFUTES-only version and dropped — `build_llm_verification_parquet.R`'s own parser forces `llm_label` to `NOT_ENOUGH_INFO` whenever `sufficient_evidence` is `FALSE` (both at parse time and again after the verbatim-quote demotion), so `llm_agrees == TRUE` already implies `sufficient_evidence == TRUE` for every row; a 4th level would always equal the 3rd. Reuses `build_nli_overview_data.R`'s exact doi-lookup join (neither `nli_scores_evidence` nor `llm_verification/scores` stores `doi`). Caches `funnel_overall`, `funnel_by_bm`, and the level-3 detail rows in one rds (`output/tables/<label>_funnel_data_<id>.rds`, `label` lowercased), same read-once-cache-everything shape as `nli_overview_data`. |
| `R/build_label_funnel_figures.R` | `refutes_funnel_figures`, `supports_funnel_figures` | Three PNGs per assessment from the cached rds: an overall 3-bar funnel (counts + % of level 1), a per-BM grouped bar chart (log-scale y, faceted by KM) showing all 3 levels side by side, and a **normalized** variant of the same per-BM chart where each level is divided by that BM's own level-1 count (a `geom_hline(yintercept = 1)` marks "full corpus") — lets BMs be compared on conversion rate regardless of raw corpus size, since the log-scale version alone makes that visually hard to read off. |
| `R/build_label_funnel_tables.R` | `refutes_funnel_tables`, `supports_funnel_tables` | One `DT::datatable()` table per assessment (level-3 detail: works where `llm_agrees == TRUE`), with a BM dropdown filter (`filter = "top"` + `bm` cast to `factor`) and a work-link column (DOI preferred, OpenAlex fallback — the same `work_link()` helper as `build_nli_bm_explorer.R`'s drill-down table). Calls `DT::datatable()` directly rather than through this project's usual `IPBES.R::table_dt()` wrapper, since `table_dt()`'s `...` lands inside `options = list(...)` and can't set the top-level `filter` argument `filter = "top"` needs. `filter = "top"` renders a filter widget for **every** column passed to `datatable()`, not just `bm` — so the displayed column set is deliberately curated down (dropping columns that are constant given the pre-filtering, e.g. `assessment`/`nli_route`, or redundant given `llm_agrees == TRUE`'s own guarantee, e.g. `sufficient_evidence`) rather than adding JS to hide the extra widgets. |
| `R/build_fig_pub_per_year.R` | `fig_pub_per_year` | Number of new publications per year, grouped by background message, among papers citing the key papers (the forward/discovery direction of the snowball search). Reads `works_citing_parquet` directly (`relation == "citing"` is already the forward direction). `works_citing_path` arrives as a vector — one directory per assessment branch, consumed here without a `pattern` so `targets` aggregates all branches into one call — and since each element's own root (`assessment=<id>`) isn't surfaced by Arrow as a column when it's the dataset's own root, `assessment` is parsed back out of the path per element. |
| `R/build_overlap_key_paper_table.R` | `overlap_key_paper_table` | Overlap table: key/seed papers (`works_parquet`) referenced in more than one background message. `works_parquet` has one row per `(paper, km, bm)` combination (a paper cited in N BMs appears N times via a many-to-many join against refs), so grouping by `id` and counting distinct km/bm pairs directly gives the overlap count — no snowball data needed. Includes an `author` column (safe here, unlike the two `overlap_after_2018_*` variants below, since `works_parquet` comes from one `pro_fetch()` call per assessment with one consistent schema). Writes a DT-table HTML to `output/tables/overlap_key_paper.html`. |
| `R/build_overlap_after_2018_sub_messages_table.R` | `overlap_after_2018_sub_messages_table` | Overlap table: citing papers (`works_citing_parquet`) published after `cutoff_year` (default 2018), referenced from more than 5 background messages. Named "sub_messages" for continuity with the legacy report; the active pipeline has no sub-message level, so this and the background-messages variant group identically (by km/bm) — kept as a separate target only for report-section continuity, not because the two computations differ. No `author` column: `works_citing`'s `authorships` struct column has inconsistent nested schemas across different (km, bm) batches within the same assessment (each `pro_snowball()` call infers its own schema — see `build_snowball_parquet.R`), so collecting it across batches errors ("Some attributes are incompatible" from `vctrs`). |
| `R/build_overlap_after_2018_background_messages_table.R` | `overlap_after_2018_background_messages_table` | Identical grouping to the "sub_messages" variant above, but additionally carries the `abstract` column, matching the legacy report's background-message variant. Same `author`-column omission and reasoning as `build_overlap_after_2018_sub_messages_table.R`. |
| `R/build_nli_bm_explorer.R` | `nli_bm_explorer_html` | Interactive plotly widget per assessment with **three** linked controls — a BM-select dropdown (default "All BMs"), a minimum-confidence slider (0-0.9, default 0.5), and a table-label dropdown (REFUTES/SUPPORTS/NOT_ENOUGH_INFO, default REFUTES). The BM dropdown + slider jointly pick one precomputed `(bm, threshold)` cell for the three chart panels; all three controls together pick one `(bm, threshold, label)` cell for a drill-down table below the chart. All works are always shown in the charts (no filtering); the threshold instead splits every bar into two **stacked** segments via `barmode = "stack"`: confidence-≥-threshold (solid fill, bottom, hover shows `label: xx.xx%`) and confidence-<-threshold (hollow/outline-only via transparent `marker.color` + colored `marker.line`, on top) — label distribution (%) splits each label's bar this way (each bar also labelled `n / N` via `text`/`textposition = "outside"` on the top trace — `format(..., trim = TRUE)` is required there, since `format()` on a multi-element vector right-pads shorter numbers to a common width by default, which otherwise leaks stray spaces into the label), the confidence-histogram panel splits each bin by its own mid vs threshold (plus a dashed threshold line), and the alignment-histogram panel splits each bin by how many of its works meet the threshold (a genuine cross-tab, computed via two separate `hist()` calls on the above/below subsets sharing the same breaks). All three controls are `method = "skip"` (native dropdown/slider UI + active-index tracking, no automatic restyle) and a small `htmlwidgets::onRender()` JS handler reads all three current indices from the widget's own layout and applies one combined `Plotly.restyle`/`Plotly.relayout` for the charts, plus swaps a precomputed HTML string into a plain `<div>`/`<table>` appended right after the chart in the DOM for the drill-down table (work linked to its DOI when one exists, else OpenAlex, both real clickable `<a>` tags; confidence; alignment; sorted by confidence descending, capped at 50 rows with the true match count always in the caption) and wires a "Download table (CSV)" button. The download is deliberately **not** limited to the 50 on-screen rows: it draws from a separate, coarser-grained precomputed grid — one array per `(bm, label)` (93 buckets for GA1's 30 BMs), NOT per `(bm, threshold, label)` — because a threshold is just a `confidence >=` cutoff on an already-confidence-sorted list, so storing it once per `(bm, label)` and slicing off the leading run client-side (via `downloadRowsFor()`) avoids re-embedding the same rows once per threshold step; a first version that naively stored per `(bm, threshold, label)` cell (930 of them) produced a 166MB file for GA1 alone, confirmed by direct measurement, before this redesign brought it down to ~37MB. Each `(bm, label)` array is capped at `download_row_cap` (5000, well above `table_row_cap`) and includes `Assessment` and `BM` as explicit CSV columns (`BM` carried per-row, since under "All BMs" each row belongs to a different actual BM) — exact match counts per `(bm, threshold, label)` (needed to flag truncation accurately even though the full row data isn't stored per-threshold) are reused cheaply from the existing `lab_above_n` stats already computed for the bar-chart panels (`match_counts_flat`), re-indexed from `label_levels` order into `label_sel_options` order to match the dropdown. The table is deliberately **not** a plotly `type = "table"` trace: two real bugs were found there by isolated repro before this design was adopted — (1) plotly table cells render any HTML they're given as escaped plain text (an `<a href=...>` shows up as literal angle brackets, not a link — so DOI links would never have been clickable), and (2) calling `add_trace()` repeatedly on an already-`subplot()`-merged figure (as opposed to a fresh `plot_ly()` object) leaves each new trace's internal plotly "attrs" bookkeeping entry unnamed, which crashed `htmlwidgets::saveWidget()`'s `shouldEval()` validation once mixed with the properly-named bar-trace attrs at realistic scale (30+ BMs); a naive splice-in-a-fresh-object workaround for that also introduced a silent off-by-one (a phantom placeholder trace from the bare `plot_ly()` base call shifted every table index by one — every dropdown/slider combination would have shown the wrong table). The DOM-table redesign sidesteps all of this. Per-cell HTML uses CSS classes (`.nli-tbl` etc., injected once into `<head>`) rather than repeating inline `style=` on every cell — at up to 50 rows x ~900 cells that repetition alone had roughly doubled the saved file size. Verified end-to-end against a real headless-Chrome DOM (`chromote`) at full production scale (30 BMs, ~1.9M rows): all three controls combine correctly (changing any one preserves the other two), marker fill/outline styling and n/N counts are exactly as intended, DOI links are genuine clickable anchors (`href`/`textContent` checked directly, not just "does it render"), the download button's `Blob` was intercepted and its CSV content verified row-for-row against the visible table, and a screenshot confirmed no layout overlap between the controls, annotations, panel titles, and charts. `save_nli_bm_explorer()` writes the figure as a self-contained standalone HTML (`htmlwidgets::saveWidget(selfcontained = TRUE)`) to `output/tables/nli_bm_explorer_<id>.html`, embedded in the report via `<iframe>` — deliberately **not** printed in-place via `cat(knitr::knit_print(widget))` inside the report's `results: asis` loop, which was tried first and found to not propagate the plotly.js dependency under Quarto's HTML output (empty div, no chart); the iframe/saveWidget route matches the existing convention for other interactive artifacts (e.g. `build_overlap_key_paper_table`'s DT tables). |
| `R/resolve_citations.R` | *(orphaned — no active target)* | Replace `(Author, Year)` citations with OpenAlex W-IDs; legacy `resolved_sections_parquet` builder, no longer wired in `_targets.R` |
| `R/build_fulltext.R` | *(orphaned — no active target)* | Download Grobid XML or PDF for each work in `works_citing`; legacy `fulltext_files` builder, no longer wired in `_targets.R` |

### Terminology

- **KM** = Key Message (e.g. `"A."`)
- **BM** = Background Message (e.g. `"A1"`)
- **SM** = Sub-Message
- **LOD** = Linked Open Data — IPBES assessments in RDF/Turtle format
- **key papers** = GA1 references used as snowball seeds (`oa_input == TRUE` in QMD nodes)
- **non-key papers** = papers discovered via snowball (`oa_input == FALSE`)

### Gitignored Intermediate Outputs

- `output/LoD/` — cached TTL files
- `output/refs/` — DB1 parquet (refs with Zotero group/key citations)
- `output/sections/` — DB2 parquet (section content; now includes `sm` column linking each SubChapter to the SubMessage that references it)
- `output/key_messages/` — DB3 parquet (KM, BM, and SM descriptive text with confidence flags)
- `output/resolved_sections/` — sections with `(Author, Year)` replaced by OpenAlex W-IDs
- `output/zotero/` — Zotero parquet dataset partitioned by assessment/group id/page
- `output/works/` — OpenAlex works parquet dataset partitioned by assessment/km/bm
- `output/snowball/` — snowball parquet datasets: nodes (assessment/km/bm/relation), edges (assessment/km/bm/edge_type), keypaper (assessment/km/bm)
- `output/works_citing/` — papers citing the seed works, partitioned by assessment/km/bm
- `output/nli_scores/` — NLI SUPPORTS/REFUTES/NEI scores per citing work, partitioned by assessment/km/bm/claim_id
- `output/nli_scores_evidence/` — active evidence-segmented NLI scores, same schema, partitioned by granularity/nli_config/assessment/km/bm/claim_id
- `output/llm_candidate_scope/` — per-claim citing-work match set feeding the `direct_evidence_match` tag, partitioned by assessment/km/bm
- `output/llm_verification/raw/` — Phase 2's resumable per-`(km, bm, claim_id, work_id)` JSON cache, partitioned by model/prompt hash/assessment
- `output/llm_verification/scores/` — Phase 2 LLM verification of NLI-flagged pairs (each tagged `direct_evidence_match`), partitioned by llm_config/assessment/nli_route/km/bm
- `output/llm_verification/raw_keypaper/`, `output/llm_verification/scores_keypaper/` — full-coverage key-paper LLM verification (every key paper, irrespective of NLI label/confidence), same shape as the two above but never collides with or invalidates them
- `output/claim_completion/raw/` — `atomic_bm`'s resumable per-fragment LLM-completion cache (`R/build_claim_completion.R`), partitioned by model/prompt hash
- `output/fulltext/` — *(no longer produced; gitignored to keep stale dirs out of commits)* per-work Grobid XML/PDF/sentinel files from the orphaned `build_fulltext.R`
- `output/resolved_sections/` — *(no longer produced)* sections with `(Author, Year)` replaced by OpenAlex W-IDs from the orphaned `resolve_citations.R`
- `output/snowballs/` — per-paper snowball `.rds` files (QMD pipeline)
- `output/nodes/`, `output/edges/` — parquet datasets (QMD pipeline)
